# BACKLOG — Vision-EviDex

Ideas, enhancements, and discovered work that doesn't fit in the current sprint. Add an entry whenever something comes up mid-work that shouldn't derail the task at hand.

**Format:** `YYYY-MM-DD · [SPRINT-DISCOVERED] · IDEA · PRIORITY · NOTES`

---

| Date | Sprint | Idea | Priority | Notes |
|---|---|---|---|---|
| 2026-05-12 | Phase 2 Wk 10 polish | `PM-11` 20 MB project size cap (warning at 15 MB, block at 20 MB) — deferred from P1 W6 | P1 | Storage warning toast already fires at 75% (`PROJECT_SIZE_BUDGET_BYTES` in `ipc-router.ts`); the hard block + UI affordance ("you must end this session" modal) never landed. Tied to Risk R-14 ("20 MB cap hit unexpectedly early"). Cleanest landing point is Phase 3 Wk 11 alongside the Template Engine work — both touch `EvidexContainerService.getSizeBytes()` and the storage-warning path. Tracking now so Asus's manual session doesn't surprise us with an oversized container that won't open. |
| 2026-05-05 | Phase 2 Wk 7 | `[PH2-REACT-TESTS]` React component test suite (SessionIntakeModal / CaptureThumbnail / GallerySkeleton) | P2 | Needs (Asus-side install): `@testing-library/react` `^16`, `@testing-library/user-event` `^14`, `@testing-library/jest-dom` `^6`, `jsdom` `^25`. Needs vitest env split (main-process specs stay node, renderer specs go jsdom — file-pattern match in `vitest.config.ts`). Needs `__tests__/setup.ts` with `window.evidexAPI` global mock + `getComputedStyle` shim. Lands as one focused PR after PH2-W8. Until then, the renderer specs added in this session (`nav-store`, `session.store`) shim window-global manually and run in node env. |
| 2026-05-05 | Phase 2 Wk 7 | `[PH2-ROUTING]` Migrate from `useNavStore` page-dispatch to `HashRouter` + React Router v6 | P1 | `react-router-dom` `^6.26.1` already installed. Component contracts (`projectId`/`sessionId` as props) are already decoupled from the store — migration is mechanical when the sprint slot opens. Do before Phase 3 (report builder needs deep-link to specific session). |
| 2026-05-05 | Phase 2 Wk 7 | `metrics.service.ts` naming mismatch vs spec (`metrics-import.service.ts`) | P2 | CLAUDE.md §3 says `MetricsImportService` lives at `metrics-import.service.ts`; the file currently on disk is `metrics.service.ts`. Reconcile before Phase 3 Wk 13 (Metrics Import implementation): either rename the file + update imports, or document the alias in CLAUDE.md §3. Do NOT create a duplicate. |
| 2026-05-05 | Phase 2 Wk 7 | 20 hard-coded hex colours in `BrandIcons.tsx` | P3 | Snapshot 2026-05-05 found `#0078D4`, `#00B4D8`, `#60CDFF`, `#0E3D7A`, `#051D3D`, `#6B2FBA`, `#FFFFFF` as gradient stops inside inline SVG markup. Likely intentional brand-art exception, but confirm during the deferred FUI-5 cleanup whether these stay as art or move to CSS gradient tokens. |
| 2026-04-18 | Phase 0 | Security remediation plan for npm audit findings (`xlsx`, `canvas/tar` chain) | P1 | `npm audit --omit=dev` reports 8 vulns (5 high). Do not run `npm audit fix --force` because it breaks locked `fabric@5.3.0`. Track mitigation before Phase 3 metrics import implementation and before release hardening. |
| 2026-04-18 | Phase 0 | Phase 1 Week 3 security gate — IPC validation integration test | P1 | Before first IPC handler merges, add a vitest integration test that invokes a handler with invalid payload and asserts it returns `IpcResult` `{ok:false, error.code:'VALIDATION_FAILED'}` rather than throwing. Covers architectural rules #2 and #3 from CLAUDE.md. |
| 2026-04-18 | Phase 0 | Phase 1 Week 4 security gate — crypto & SQL audit | P1 | When `EvidexContainerService` (AES-256-GCM) and `DatabaseService` land, audit: random IV per encrypt, auth-tag verification on decrypt, documented PBKDF2 iteration count, and that every SQL call uses `db.prepare(...)` with bound params (rule #4). Block merge until reviewed. |
| 2026-04-18 | Phase 0 | Phase 2 Week 9 / Phase 3 security gate — fabric SVG XSS decision point | P2 | Before any fabric `.toSVG()` path ships (annotation export / HTML export), either sanitize SVG output or re-open the `fabric@5.3.0` pin decision against advisory GHSA-hfvx-25r5-qc3w (CVSS 7.6). Current reachability is zero; becomes reachable the moment annotation export lands. |
| 2026-04-17 | Phase 0 | Resolve CTS corporate SSL cert for node-gyp | P2 | Currently a Phase 0 open item. Asus TUF covers builds, so not blocking. Fix when someone wants to run `npm run dev` on CTS. |
| 2026-04-17 | Phase 0 | Ship bundled Inter font files | P1 | Tokens reference `var(--font-sans)` which falls back to system-ui. Add `Inter-*.ttf` to `src/renderer/fonts/` with `@font-face` in `global.css` before Phase 1 Week 3 sign-off. |
| 2026-04-17 | Phase 0 | Pick real Keygen.sh SDK (or fetch + JWT) | P2 | `@keygen-sh/keygen-js` wasn't a real npm package. Phase 1 Week 4 picks between a maintained community SDK and rolling the HTTP + RSA signature verification by hand with `jose` or `node:crypto`. |
