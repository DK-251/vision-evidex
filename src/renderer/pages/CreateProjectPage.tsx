/**
 * S-11 — New project form: name, client, template, branding, storage path
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function CreateProjectPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Create Project</h1>
      <p className="text-sm text-text-secondary">S-11 — New project form: name, client, template, branding, storage path</p>
    </div>
  );
}
