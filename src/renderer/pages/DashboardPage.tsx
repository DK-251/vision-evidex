/**
 * S-03 — Dashboard: metrics panel, quick links, recent projects, session indicator
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function DashboardPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Dashboard</h1>
      <p className="text-sm text-text-secondary">S-03 — Dashboard: metrics panel, quick links, recent projects, session indicator</p>
    </div>
  );
}
