/**
 * S-24 — Full keyboard shortcut reference
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function KeyboardShortcutsPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Keyboard Shortcuts</h1>
      <p className="text-sm text-text-secondary">S-24 — Full keyboard shortcut reference</p>
    </div>
  );
}
