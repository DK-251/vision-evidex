/**
 * S-01 — Licence activation (standard mode only, skipped in no-licence mode)
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function LicenceActivationPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Licence Activation</h1>
      <p className="text-sm text-text-secondary">S-01 — Licence activation (standard mode only, skipped in no-licence mode)</p>
    </div>
  );
}
