/**
 * S-18 — Excel/JSON metrics import with schema version validation
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function MetricsImportPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Metrics Import</h1>
      <p className="text-sm text-text-secondary">S-18 — Excel/JSON metrics import with schema version validation</p>
    </div>
  );
}
