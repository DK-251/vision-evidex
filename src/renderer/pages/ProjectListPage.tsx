/**
 * Recent / all projects browser
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function ProjectListPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Project List</h1>
      <p className="text-sm text-text-secondary">Recent / all projects browser</p>
    </div>
  );
}
