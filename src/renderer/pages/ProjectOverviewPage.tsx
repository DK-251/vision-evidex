/**
 * S-13 — Project home: sessions, metrics, captures, size usage
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function ProjectOverviewPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Project Overview</h1>
      <p className="text-sm text-text-secondary">S-13 — Project home: sessions, metrics, captures, size usage</p>
    </div>
  );
}
