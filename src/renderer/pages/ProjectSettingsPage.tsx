/**
 * S-12 — Rename, update client, change template, update branding profile
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function ProjectSettingsPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Project Settings</h1>
      <p className="text-sm text-text-secondary">S-12 — Rename, update client, change template, update branding profile</p>
    </div>
  );
}
