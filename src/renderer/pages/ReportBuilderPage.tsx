/**
 * S-17 — Live preview, section reorder, metrics import, export bar
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function ReportBuilderPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Report Builder</h1>
      <p className="text-sm text-text-secondary">S-17 — Live preview, section reorder, metrics import, export bar</p>
    </div>
  );
}
