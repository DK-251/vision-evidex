/**
 * S-10 — Single session: gallery, intake data, pass/fail summary
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function SessionDetailPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Session Detail</h1>
      <p className="text-sm text-text-secondary">S-10 — Single session: gallery, intake data, pass/fail summary</p>
    </div>
  );
}
