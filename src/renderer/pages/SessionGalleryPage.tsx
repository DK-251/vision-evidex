/**
 * S-06 — Live capture gallery during an active session
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function SessionGalleryPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Session Gallery</h1>
      <p className="text-sm text-text-secondary">S-06 — Live capture gallery during an active session</p>
    </div>
  );
}
