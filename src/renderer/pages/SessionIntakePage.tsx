/**
 * S-04 — Session intake form: test ID, scenario, tester, environment
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function SessionIntakePage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Session Intake</h1>
      <p className="text-sm text-text-secondary">S-04 — Session intake form: test ID, scenario, tester, environment</p>
    </div>
  );
}
