/**
 * S-09 — All sessions across a project with intake summary
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function SessionListPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Session List</h1>
      <p className="text-sm text-text-secondary">S-09 — All sessions across a project with intake summary</p>
    </div>
  );
}
