/**
 * S-22 — Formal sign-off record: accept/reject/with-comments, append-only
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function SignOffPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Sign Off</h1>
      <p className="text-sm text-text-secondary">S-22 — Formal sign-off record: accept/reject/with-comments, append-only</p>
    </div>
  );
}
