/**
 * S-19 — One-click DSR + WSR aggregator
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function StatusReportsPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Status Reports</h1>
      <p className="text-sm text-text-secondary">S-19 — One-click DSR + WSR aggregator</p>
    </div>
  );
}
