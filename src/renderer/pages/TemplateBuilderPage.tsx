/**
 * S-15 — Drag-and-drop canvas, block palette, branding selector
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function TemplateBuilderPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Template Builder</h1>
      <p className="text-sm text-text-secondary">S-15 — Drag-and-drop canvas, block palette, branding selector</p>
    </div>
  );
}
