/**
 * S-14 — 5 pre-built templates + custom library (clone/delete)
 * Phase 0 stub — real implementation arrives per sprint plan.
 */
export function TemplateLibraryPage(): JSX.Element {
  return (
    <div className="p-6 text-text-primary">
      <h1 className="text-xl font-semibold mb-2">Template Library</h1>
      <p className="text-sm text-text-secondary">S-14 — 5 pre-built templates + custom library (clone/delete)</p>
    </div>
  );
}
